# ========================================
# Frontend Configuration
# ========================================
#
# IMPORTANT: Copy this file to .env
# Command: cp .env.example .env
#
# Note: Vite requires environment variables to be prefixed with VITE_
# to be exposed to the frontend code
#
# ========================================

# ========================================
# API CONFIGURATION
# ========================================

# Backend API base URL
# This is where the frontend will send API requests
#
# Local development:
VITE_API_BASE_URL=http://localhost:8000/api/v1
#
# Docker development (if frontend runs in Docker):
# VITE_API_BASE_URL=http://api:8000/api/v1
#
# Production (replace with your domain):
# VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
#
# Note: Must include /api/v1 path if your backend uses this prefix

# ========================================
# APPLICATION SETTINGS
# ========================================

# Application name (displayed in header, title, etc.)
VITE_APP_NAME=Auto-Blog SEO Monster

# Application version
VITE_APP_VERSION=1.0.0

# Environment (development, staging, production)
VITE_APP_ENV=development

# ========================================
# FEATURE FLAGS (Optional)
# ========================================

# Enable/disable features in the frontend
VITE_FEATURE_ANALYTICS=false
VITE_FEATURE_DARK_MODE=true
VITE_FEATURE_NOTIFICATIONS=true

# ========================================
# THIRD-PARTY INTEGRATIONS (Optional)
# ========================================

# Google Analytics (if enabled)
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry (error tracking)
# VITE_SENTRY_DSN=https://xxx@sentry.io/xxx

# PostHog (product analytics)
# VITE_POSTHOG_API_KEY=phc_xxx
# VITE_POSTHOG_HOST=https://app.posthog.com

# ========================================
# UI CONFIGURATION (Optional)
# ========================================

# Default theme
VITE_DEFAULT_THEME=light

# Enable debug mode
VITE_DEBUG=false

# ========================================
# HELPFUL NOTES
# ========================================
#
# - All environment variables must start with VITE_ to be accessible
# - Variables are embedded at build time (not runtime)
# - Rebuild frontend after changing variables: npm run build
# - Access in code: import.meta.env.VITE_API_BASE_URL
#
# Example usage in code:
#   const apiUrl = import.meta.env.VITE_API_BASE_URL;
#   console.log('API URL:', apiUrl);
#
# ========================================
